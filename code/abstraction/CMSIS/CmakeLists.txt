target_sources (
    ${PROJECT_NAME}.elf
    PRIVATE ${CMAKE_CURRENT_LIST_DIR}/src/_cmsis_init_.c
)

target_include_directories (
    ${PROJECT_NAME}.elf
    PUBLIC  ${CMAKE_CURRENT_LIST_DIR}/Include
            ${CMAKE_CURRENT_LIST_DIR}/Device/ST/STM32G4xx/Include 
)